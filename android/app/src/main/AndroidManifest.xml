<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- Location needed for Cast discovery on Android 12 and below (mDNS) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="32" />
    <!-- Android 13+ nearby devices permission for Cast discovery -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"
        tools:ignore="ScopedStorage" />
    <!-- Android 13+ Permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Background Audio Playback -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- TV-specific features -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- Declare this app is for TV -->
    <uses-feature
        android:name="android.hardware.type.television"
        android:required="false" />

    <!-- allowBackup is disabled: auth tokens (JWT access+refresh) live in
         DataStore and auto-backup would carry them into cloud transfers and
         adb backups. A media client has nothing worth backing up. -->
    <application
        android:name=".TelePlayApp"
        android:allowBackup="false"
        android:requestLegacyExternalStorage="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:banner="@drawable/app_banner"
        android:supportsRtl="true"
        android:theme="@style/Theme.TelegramTV"
        android:usesCleartextTraffic="true"
        android:largeHeap="true"
        tools:targetApi="31">

        <!-- Main Activity - TV Launcher (intent-filter in tv/AndroidManifest.xml) -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
            android:banner="@drawable/app_banner"
            android:configChanges="keyboard|keyboardHidden|navigation|screenSize|screenLayout|uiMode"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="stateHidden|adjustNothing">
        </activity>

        <!-- Mobile Activity - Mobile Launcher (intent-filter in mobile/AndroidManifest.xml) -->
        <activity
            android:name=".ui.mobile.MobileMainActivity"
            android:exported="true"
            android:theme="@style/Theme.Aruvi.Mobile"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PATHS"
                android:resource="@xml/file_paths" />
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Background Audio Playback Service -->
        <service
            android:name=".service.AudioPlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- Background Download Service -->
        <service
            android:name=".service.DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Chromecast - Media3 Default Options Provider -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.aruvi.tir.cast.CastOptionsProvider" />
    </application>

</manifest>
